#### To see the docker network List
`docker network ls`

---

### Create a new Network
`sudo docker network create <name>`

---

### Add Containers to a Network 
`sudo docker run -itd -- network <network_name> --name <container_name> <image_name>`


---